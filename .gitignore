# Build output
target/
build/
*.jar
*.war

# The config-repo is its own Git repository, not a directory of this one (Section 6).
# infra/bootstrap.ps1 initialises it; in staging/prod it is a real remote the Config Server clones.
/config-repo/

# Local environment credentials
infra/.env
.env
# Per-environment copies: infra/.env.contabo holds the deployed box's real secrets, pulled down so
# they can be read and edited without an SSH session. The wildcard is deliberately broad — a file
# named after an environment is a file full of that environment's credentials, and the two
# .example negations below are the only things that should ever be committed.
.env.*
infra/.env.*
!.env.example
!infra/.env.example
!.env.dev.example
!infra/.env.dev.example

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# Flutter / Dart
clients/**/.dart_tool/
clients/**/build/
clients/**/.flutter-plugins
clients/**/.flutter-plugins-dependencies
clients/**/ios/Pods/
clients/**/android/.gradle/
*.g.dart
*.freezed.dart

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# services/order-manager is its own repository (delivery.order-manager) with its own pipeline, the
# same way config-repo is. Without this git records it as a bare gitlink with no .gitmodules, and a
# fresh checkout of THIS repo gets an empty directory that the Maven reactor then cannot read.
/services/order-manager/

# Kotlin compiler session files. Written and deleted while Gradle runs, so a `git add` racing a
# build fails on a path that existed a moment earlier.
clients/**/android/.kotlin/
