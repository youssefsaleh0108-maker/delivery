name: accounting-service

on:
  push:
    branches: [main]
    paths:
      - 'services/accounting-service/**'
      - 'platform/**'
      - 'pom.xml'
      - '.github/workflows/reusable-service-ci.yml'
      - '.github/workflows/accounting-service.yml'
  pull_request:
    paths:
      - 'services/accounting-service/**'
      - 'platform/**'
      - 'pom.xml'

jobs:
  ci:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service-name: accounting-service
      service-path: services/accounting-service
    secrets: inherit
