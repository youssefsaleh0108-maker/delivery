name: push-connector

on:
  push:
    branches: [main]
    paths:
      - 'services/push-connector/**'
      - 'platform/**'
      - 'pom.xml'
      - '.github/workflows/reusable-service-ci.yml'
      - '.github/workflows/push-connector.yml'
  pull_request:
    paths:
      - 'services/push-connector/**'
      - 'platform/**'
      - 'pom.xml'

jobs:
  ci:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service-name: push-connector
      service-path: services/push-connector
    secrets: inherit
