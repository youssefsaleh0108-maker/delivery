name: push-worker

on:
  push:
    branches: [main]
    paths:
      - 'services/push-worker/**'
      - 'platform/**'
      - 'pom.xml'
      - '.github/workflows/reusable-service-ci.yml'
      - '.github/workflows/push-worker.yml'
  pull_request:
    paths:
      - 'services/push-worker/**'
      - 'platform/**'
      - 'pom.xml'

jobs:
  ci:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service-name: push-worker
      service-path: services/push-worker
    secrets: inherit
